class Program {
    int calls;
    int ackermann(int m, int n){
        calls = calls + 1;
        if(m==0){ return n+1; }
        if(n==0){ return ackermann(m-1, 1);  }
        else{ return ackermann(m-1, ackermann(m, n-1)); }


    }

    int main(){

    }
}